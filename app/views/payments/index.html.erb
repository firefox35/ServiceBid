<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" href="../../favicon.ico">

    <title>List of Payments</title>

    <!-- Bootstrap core CSS -->
    <link href="../../dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- IE10 viewport hack for Surface/desktop Windows 8 bug -->
    <link href="../../assets/css/ie10-viewport-bug-workaround.css" rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="assets/stylesheet/payments.css" rel="stylesheet">
      </head>
    <body>
  
<div id="fix">
<h1>Listing Payments</h1>

<table>
  <thead>
    <tr>
      <th>Mastercard</th>
      <th>Visa</th>
      <th>Visa debit</th>
      <th>Job offer</th>
      <th>Customer name</th>
      <th>Card number</th>
      <th>Expiry date</th>
      <th>Three digit pin number</th>
      <th>Amount</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @payments.each do |payment| %>
      <tr>
        <td><%= payment.mastercard %></td>
        <td><%= payment.visa %></td>
        <td><%= payment.visa_debit %></td>
        <td><%= payment.job_offer_id %></td>
        <td><%= payment.customer_name %></td>
        <td><%= payment.card_number %></td>
        <td><%= payment.expiry_date %></td>
        <td><%= payment.three_digit_pin_number %></td>
        <td><%= payment.amount %></td>
        <td><%= link_to 'Show', payment %></td>
        <td><%= link_to 'Edit', edit_payment_path(payment) %></td>
        <td><%= link_to 'Destroy', payment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Back', dealers_path %>
</div>
</body>
</html>